name: pos_294_backend
description: Database API proxy server for pos_294 Flutter web client
version: 1.0.0

environment:
    sdk: ^3.12.2

dependencies:
    mysql_client: ^0.0.27
    shelf: ^1.4.1

dev_dependencies:
    lints: ^3.0.0
